/**
 * AJAX: asynchronous javascript XML(data ka format hai)
 *  
 * XHR::xml http request
 * 
 * HTTP se client and server ke bich ki communication hoti h
 * client and server ke bich baat krne ka tariika http h 
 * http ke help se baat krte h 
 * 
 * is a set of 'web development technique'
 * using many web technologies on the 'client-side'
 * to create asynchronous web applications.
 * 
 * 
 *  with AJAX, web application can send and retrieve  
 * data from a server asynchronously (in the background)
 * without interfering with the display and 
 * behavior of the existing page.
 *  
 * we dont use data in XML format anymore.
 * we use JSON now.
 *  
 * we have  3 most common ways to create and send request to server
 * 1:)xml httpRequest(old way of doing)
 * 2:)fetch api(new way of doing)
 * 3:)axios (this is third party library)
 * 
 */